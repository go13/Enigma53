				<div class="form-actions">
					<% this.qtext %>
				</div>
				<blockquote style="margin-bottom: 15px">
					<% for item in this.answers %>
					<label class="checkbox"> 
						<input type="checkbox">	<% item.atext %>
					</label> 
					<% endfor %>
				</blockquote>
